"use client"

/* ctrl/cmd+k command palette: mounted in the shell and opened through the
   `screenkit:command-palette` window event (see hotkeys.tsx). Implemented by
   its own feature; renders nothing until then. */
export function CommandPalette() {
  return null
}
