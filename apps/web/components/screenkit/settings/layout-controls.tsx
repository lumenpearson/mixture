"use client"

/* layout-controls: mounted in the settings area; implemented by its own feature. Renders
   nothing until the feature lands so the mount point already exists. */
export function LayoutControls() {
  return null
}
